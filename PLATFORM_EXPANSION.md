# PicMedic 웹/앱 확장 검토

> 2026-08-31 대화에서 나온 검토 내용을 기록한 문서. **아직 결정된 계획이 아니라
> 방향을 다듬기 위한 메모**다. 실제 착수 전에 이 문서 기준으로 다시 논의할 것.

## 배경

현재(v1) PicMedic은 PySide6(Qt) 기반 데스크톱 앱. 여기서 나온 질문: "웹 버전과
앱 버전도 만들 수 있을까?" — 서버 없이, 지금 v1처럼 로컬에서 완결되는 형태로.

## 지금 코드베이스에서 재사용 가능한 것 / 아닌 것

- **`core/`(진단·복구 엔진)는 Qt에 전혀 의존하지 않는 순수 Python.** 실제로
  `PySide`/`gui` import가 하나도 없음을 확인함(`analyzer.py`, `converter.py`,
  `detector.py`, `scanner.py` 전부). → **그대로 재사용 가능.**
- **`gui/`(화면 전체)는 100% PySide6 전용.** 웹/모바일 어디에도 이식 불가 —
  화면은 새로 만들어야 함.

## 검토한 방향

### 웹 버전 — "브라우저에서 URL만 치면 되는" 버전은 이 앱과 안 맞음

이 앱의 정체성이 "로컬 폴더를 통째로 재귀 스캔"이라, 브라우저 샌드박스로는
그대로 옮기기 어렵다.

1. **업로드형 웹앱** (사진을 서버에 올려서 처리) — 기술적으로는 `core/`를
   FastAPI 등으로 감싸기만 하면 되지만, 수만 장짜리 라이브러리를 업로드해야
   하고 "원본을 함부로 안 건드린다"는 이 앱 원칙과도 어긋남. **기각.**
2. **브라우저 내 완전 클라이언트 사이드(Pyodide/WASM)** — `core/`가 Pillow
   기반이라 이론상 가능해 보이지만, **HEIC 지원(`pillow-heif` → `libheif`)이
   WASM으로 컴파일되어 있지 않아 브라우저 안에서 못 돌아간다.** HEIC는 이 앱의
   대표 기능이라 이 제약이 치명적. JPEG/PNG/WEBP/GIF/TIFF/BMP만 된다면 이론상
   가능하지만 기능이 반쪽만 남음. **HEIC를 포기할 게 아니면 기각.**
3. **로컬 상주 + 웹 UI (서버 없음)** — 아래 "채택한 방향" 참고. **✅ 이걸로 검토
   확정.**

### 앱(모바일) 버전 — 사실상 완전히 새로운 프로젝트

- `core/`(Pillow 기반)가 iOS/Android에서 그대로 안 돌아감. 재구현이 필요하고,
  Chaquopy 같은 파이썬 브릿지도 이런 용도로 프로덕션 사례가 많지 않음.
- 더 근본적으로 모바일은 "폴더를 골라서 재귀 스캔"이 안 됨(특히 iOS) — 사진
  앱(Photos/MediaStore) 피커를 통해서만 접근 가능해서, 지금 핵심 흐름(폴더
  선택 → 전체 스캔)과 다른 UX를 새로 설계해야 함.
- **결론: 우선순위 뒤로 미룬다.** 웹 버전(로컬 상주형)이 자리 잡은 뒤에 별도
  프로젝트급으로 재검토.

## 채택한 방향: pywebview 기반 "로컬 상주 + 웹 UI"

**서버가 뜨지 않는다** — OS에 이미 내장된 웹뷰(Windows는 WebView2, macOS는
WebKit)를 파이썬이 그대로 띄우고, JS ↔ Python 호출은 pywebview가 제공하는
내부 브릿지로 처리한다(HTTP 서버 아님, `localhost:포트` 여는 방식도 아님).

- **`core/`는 한 줄도 안 고치고 재사용.** HEIC 포함 지금 기능 그대로 동작
  (로컬 파이썬이 도는 거라 `pillow-heif` 제약이 없음).
- **새로 만들 것은 화면(HTML/CSS/JS)뿐.** 지금 Qt 화면 구조(검사 결과, 복구
  화면, 각종 팝업)를 웹 UI로 그대로 옮기는 작업 — 로직은 그대로, 겉모습만 교체.
- **패키징도 지금처럼.** PyInstaller로 실행 파일 하나(`.exe`/`.app`)로 묶을 수
  있어서 "설치 없이 실행 파일 하나" 느낌(v1 경험) 그대로 유지.
- 굳이 "브라우저 URL로 열리는 웹 버전"까지 원하면, 같은 화면(HTML/JS)을 사용자가
  로컬에서 직접 실행(`pip install` 후 커맨드 실행)해서 자기 브라우저로 여는
  방식이 현실적 — 클라우드/원격 서버는 여전히 없음, 로컷 전용.

## 다음 단계 (아직 미착수)

- [ ] pywebview로 화면 하나(예: 홈 화면)를 가볍게 프로토타입해서 감 잡아보기
- [ ] 지금 `gui/` 화면 구조(스크린별 상태·시그널 흐름)를 웹 UI로 옮길 때
      1:1로 대응시킬 방법 정리 (예: 각 QDialog/화면 전환이 웹에서는 라우팅/모달로)
- [ ] `core/`를 pywebview 백엔드에서 부르는 최소 API 설계 (예: `scan(paths)`,
      `recover(files, mode, ...)` 같은 Python 함수를 JS에서 호출)

## 관련 문서

- [DESIGN.md](DESIGN.md) — 지금 Qt 화면의 색상/아이콘/컴포넌트 패턴. 웹 UI로
  옮길 때도 같은 디자인 언어(색상 팔레트, 카드 스타일 등)를 유지하는 게 좋음.
- [PRD_MVP우선순위.md](PRD_MVP우선순위.md) — 지금 v1이 커버하는 기능 범위.
