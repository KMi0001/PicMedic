# Phase 2 — 사진 정리 상세 기획

> PRD_MVP우선순위.md의 P3 "Phase 2 — 사진 정리" (원 PRD 31장: 중복/유사 사진 탐지,
> 날짜·장소별 분류, EXIF 기반 정리, 자동 폴더 구성)를 실제로 만들기 위한 상세
> 기획서.

## ✅ 1차 범위 확정 (2026-08-31)

- **정확 중복만** (SHA-256 파일 내용 해시). 유사 중복(퍼셉추얼 해시)은 다음 단계.
- **별도 "중복 사진" 화면 신설** — 검사 결과 화면의 표/필터에 통합하지 않음.
  다만 완전히 새로운 진입점(홈 화면부터 새 스캔)은 아니고, **같은 검사 세션
  (`ScanSessionWindow`) 안에서 검사 결과 화면 → "중복 파일 보기" 버튼 →
  이 새 화면**으로 이동하는 구조. 같은 스캔에서 이미 계산한 해시를 그대로
  쓰므로 다시 스캔할 필요 없음.
- **찾아서 보여주기까지만** — 삭제/이동 액션은 이번 1차에 넣지 않음. 다음
  단계에서 별도로 설계.

## ✅ 실사용 피드백 반영 (2026-08-31, 같은 날 후속)

1차 배포 후 실제로 써보니 중복 그룹이 680개까지 나오는 경우가 있어서, 그룹마다
하나씩 "남길 파일"을 고르는 게 비현실적이라는 피드백을 받음. 그래서 두 가지를
추가로 구현:

- **삭제/이동까지 포함** — 애초 "1차는 찾아서 보여주기까지만"이었지만, 확인만
  해서는 실사용에 부족하다는 피드백에 따라 이번에 같이 구현. 완전 삭제 대신
  `utils/trash.py`로 만든 앱 전용 **임시 휴지통**(`임시휴지통/` 폴더)으로 옮기는
  방식 — 원본 보호 원칙과 절충. 이동 전 확인 팝업, 이동 후 안내 팝업 + 휴지통
  화면으로 자동 이동.
- **폴더 단위 일괄 처리** — "동일 사진이 여러 장인 문제는 보통 폴더째로
  중복(예: 백업 폴더를 통째로 복사)인 경우가 많다"는 관찰에 따라,
  `gui/duplicate_screen.py::_cluster_by_folder`로 그룹 안 파일들이 전부 서로
  다른 폴더에 있는 경우만 폴더 조합(`frozenset`)별로 묶어서, "이 폴더 남기기"
  한 번의 선택으로 관련된 모든 그룹에 일괄 적용되게 함. 같은 폴더 안에 중복이
  있는 경우(폴더만으론 구분 불가)만 기존처럼 파일별로 개별 선택. 실측: 무작위
  이미지 20쌍(폴더 A/B 교차 중복) + 같은 폴더 중복 2쌍 테스트에서 22개 그룹이
  "1개 폴더 조합(20개 그룹 일괄) + 2개 개별 확인"으로 정리됨.

## 왜 이 순서인가

[PicMedic_PRD_v2.md](PicMedic_PRD_v2.md) 1.2절이 PicMedic이 다루는 핵심 문제
목록에서 "동일 사진이 여러 개 존재하는 문제"를 Phase 2의 대표 문제로 명시하고
있음 — 즉 **중복 탐지가 Phase 2의 원래 취지에서 가장 먼저였던 항목**.

## Phase 2의 4개 항목, 실현 가능성 검토

| 항목 | 기술 난이도 | 외부 의존성 | 원칙 충돌 소지 | 1차 추천 |
|---|---|---|---|---|
| 중복/유사 사진 탐지 | 낮음~중간 | `imagehash`(선택) | 없음 | ✅ |
| 날짜별 분류 | 낮음 | 없음(EXIF 이미 읽음) | 낮음(자동 폴더 구성과 묶일 때만) | 2차 |
| 장소별 분류 | 중간~높음 | 역지오코딩(오프라인 DB 또는 인터넷) | "로컬 전용" 원칙과 충돌 가능 | 도시 단위로 축소해 2차 (2026-09-01 업데이트, 아래 새 절 참고) |
| 자동 폴더 구성 | 낮음(로직) / 설계는 신중 | 없음 | **높음** — "원본 보호" 원칙과 직접 충돌 | 2차 (복사 전용으로) |

**장소별 분류가 보류인 이유**: GPS 좌표(위도/경도)까지는 EXIF에서 바로 뽑을 수
있지만, 이걸 "서울시 강남구"처럼 사람이 읽는 지명으로 바꾸려면 역지오코딩이
필요함 — 인터넷 API를 쓰거나 오프라인 지명 DB를 앱에 내장해야 함. 둘 다 이번
1차 범위에 넣기엔 무거워서, 필요하면 위경도만 노출하는 수준으로 축소하거나
아예 다음 단계로 미룸.

## 1차 대상: 중복/유사 사진 탐지

### 범위

- **정확 중복(바이트 단위 동일 파일)**: 파일 내용 해시(SHA-256) 비교. 구현
  단순, 오탐 없음.
- **유사 중복(리사이즈/약간 편집/다시 저장된 사진)**: 퍼셉추얼 해시(perceptual
  hash) 비교 — `imagehash` 패키지(Pillow 기반, 가벼움) 사용 권장. 임계값 이하
  해밍 거리면 "유사"로 묶음.
- 두 종류를 구분해서 보여줌: "완전 중복 N그룹" / "유사 중복 N그룹".

### 데이터 모델 변경 (`models/`)

- `FileInfo`에 필드 추가: `content_hash: Optional[str]`, `perceptual_hash: Optional[str]`
- `ScanResult`에 중복 그룹 계산 메서드 추가: `duplicate_groups() -> list[list[FileInfo]]`
  (스캔이 끝난 뒤, 이미 채워진 해시들을 기준으로 그룹핑 — 스캔 자체를 다시
  돌 필요 없음)

### 스캔 로직 변경 (`core/scanner.py`, `core/analyzer.py`)

- `analyze_file`이 정상 디코딩에 성공한 파일에 한해 해시 2종을 같이 계산.
  (손상/미지원 파일은 중복 비교 대상에서 자연히 제외됨)
- 해시 계산은 이미 파일을 열어 디코딩하는 김에 같이 하므로 **추가 파일 I/O는
  없음** — 단, HEIC처럼 무거운 형식은 해시 계산 자체도 비용이 있어 체감 스캔
  시간에 영향 줄 수 있음(갭 #8 경험상 미리 염두에 둘 것 — 필요하면 "정확 중복만
  먼저, 유사 중복은 옵션으로 켜기" 식으로 나눔).

### 화면 구조 (확정)

**`gui/duplicate_screen.py` 신설** — 같은 검사 세션(`ScanSessionWindow`) 안의
새 화면. `ResultScreen`에 "중복 파일 보기" 버튼을 추가해서 이동한다(기존
"복구 가능한 파일 보기" 버튼과 같은 자리/스타일). 다시 스캔하지 않고, 이미
계산된 `content_hash`로 `ScanResult.duplicate_groups()`를 호출해 그룹만
보여준다.

- 헤더: 아이콘 + "중복 사진" 제목, 오른쪽 위에 "← 뒤로"(검사 결과 화면으로).
- 상단 요약: `SummaryChip` 재사용 — "중복 그룹" 개수, "중복 파일" 총 개수.
- 그룹 목록: 그룹마다 작은 카드로, 그룹 안 파일들(파일명 + 경로 + 크기) 나열.
  삭제/이동 버튼 없음(1차 범위 밖) — 순수 확인용.
- 중복이 하나도 없으면 "중복된 파일이 없습니다" 안내만 표시.

### 2차(이번엔 범위 밖, 다음 단계): 날짜 기반 자동 폴더 구성

- EXIF `DateTimeOriginal` 기준 "YYYY/MM" 폴더로 정리.
- **원본 보호 원칙 때문에 기본은 "복사"** — 새 폴더 구조에 복사본을 만들고,
  원본은 그대로 둠. "이동"은 명시적으로 켜야 하는 옵션 + 강한 경고 문구.
- 이미 복구/변환 흐름에 있는 `_confirm_dialog`/진행 팝업 패턴을 그대로 재사용
  가능(이동/복사 대상 파일 수가 많으면 배치 작업이라 진행률 팝업 필요).

## 2026-09-01 논의: 뷰어 우선 정리 + 위치/기기 정보

중복 사진 기능을 실사용해보면서 나온 논의. 두 가지 방향이 정해짐:

1. **"뷰어 우선, 실제 이동은 별도"를 2차 전체(날짜/위치 분류)의 기본 원칙으로
   삼는다** — 정리를 이미 잘 해둔 사용자일수록 스캔하자마자 파일이 실제로
   움직이면 오히려 혼란스러울 수 있다는 우려에서 나온 결론.
2. **장소별 분류를 "보류"에서 "도시 단위로 축소해 2차"로 승격** — 정밀 주소(동
   단위 이상)는 여전히 무리지만, 도시/국가 단위면 오프라인으로 충분히 가능하다는
   게 확인됨.

### 뷰어 우선 원칙 (날짜별 분류 + 장소별 분류 공통 적용)

- 스캔이 끝나면 **날짜/도시별로 묶은 결과를 화면에서 훑어보기만** 한다 —
  `gui/duplicate_screen.py`가 지금 하는 것과 같은 패턴(그룹 계산 → 화면에
  표시 → 실제 파일 조작은 사용자가 명시적으로 버튼을 눌러야만 발생).
  스캔 시점엔 디스크의 어떤 파일도 건드리지 않는다.
- 뷰어에서 "실제로 폴더에 정리하기"를 눌러야만 그때 파일 복사(기본)/이동
  (명시적 옵션)이 실행된다. 즉 "보여주기"와 "실행"이 완전히 분리된 2단계.
- ⚠️ **주의**: 뷰어 화면의 문구에서 "정리됨", "완료" 같은 표현을 쓰면 안 됨 —
  아직 실제 파일은 그대로인데 사용자가 "이미 옮겨진 줄" 착각할 수 있음.
  "미리보기"/"이렇게 묶여요" 같은, 실행 전 상태임이 분명한 표현만 사용.

### 장소별 분류 (도시 단위, 정밀 주소 아님)

- **범위**: GPS 좌표(EXIF `GPSInfo`) → 가장 가까운 도시 이름으로 매칭. 도로명
  주소나 "동" 단위는 이번 범위 밖(아래 이유 참고).
- **구현**: `reverse_geocoder` 패키지(오프라인, 전세계 도시 좌표 CSV 내장,
  최근접 지점 탐색 — 인터넷 불필요, "로컬 전용" 원칙 유지됨).
- **왜 도시 단위까지만인가**: 정밀 주소나 "동" 단위 행정구역까지 가려면
  나라마다 행정구역 경계 데이터(폴리곤)를 따로 구해야 해서 범위가 급격히
  커짐. 한국만 놓고 보면 공공데이터로 가능하지만, 해외 사진까지 똑같이
  하려면 나라별 데이터가 다 필요해 1차로는 무리 — 필요성이 확인되면
  "국내는 동 단위, 해외는 도시 단위"로 나누는 걸 다음 단계로 고려.
- ⚠️ **주의사항**:
  - GPS 정보가 아예 없는 사진이 훨씬 많음(위치 서비스 꺼진 상태로 촬영,
    메신저로 재전송되며 EXIF 통째로 제거된 사진, 스크린샷 등) — "위치 정보
    없음" 그룹을 항상 필수로 둬야 함. 이게 기본값이 될 가능성이 높음.
  - 최근접 좌표 매칭이라 국경/도시 경계 근처는 엉뚱한 도시로 묶일 수 있음 —
    화면에 "대략적인 위치"라고 명시해야 함.
  - `reverse_geocoder`가 PyInstaller로 Windows/macOS 양쪽에서 실제로
    패키징되는지 확인 필요(CSV 데이터 파일이 빌드에 포함되는지 `.spec`
    조정이 필요할 수 있음) — macOS는 GitHub Actions로만 빌드하므로 실제
    검증에 시간이 더 걸림.
  - 위치 데이터는 민감한 개인정보 — "로컬에서만 쓰고 어디에도 전송하지
    않는다"는 원칙을 코드 주석/문서에 명확히 남겨서, 나중에 실수로 로그나
    원격 전송 코드가 추가되지 않도록 방지.

### 기기 정보 (촬영 기기)

- EXIF `Make`/`Model` 태그를 읽어서 `FileInfo`에 필드 추가(예:
  `camera_make`, `camera_model`). 이미 EXIF를 읽는 김에 같이 뽑는 것이라
  추가 비용 거의 없음.
- ⚠️ **주의**: 스크린샷, 편집 후 재저장된 파일, 다운로드로 받은 사진 등은
  이 태그가 아예 비어있는 경우가 많음 — "알 수 없음"으로 표시하고 필수
  정보처럼 다루지 않기.

### 스크린샷 따로 묶기

- 정확한 판별은 불가능하고 휴리스틱으로 추정: 해상도가 기기 화면 해상도와
  일치하는지, 파일명이 "Screenshot_"/"스크린샷" 등 패턴에 맞는지.
- ⚠️ **주의**: 오탐/누락이 있을 수 있는 "추정" 기능임을 화면에 명시하고,
  일반 사진처럼 그룹에서 빼거나 옮길 수 있게 해서 잘못 묶여도 되돌리기
  쉽게 만들 것.

### 위치 시각화 (지도)

- `QtWebEngine` + 실제 지도 타일은 무거운 의존성 + 타일을 인터넷에서
  받아야 해서 제외. 대신 `QPainter`로 세계 윤곽선을 그리고 그 위에 도시별
  좌표를 점(원)으로 찍는 정적 지도로 충분 — 확대/이동은 안 되지만 앱
  아이콘을 그리는 것과 같은 방식이라 가볍고 의존성이 없음. 원의 크기로
  사진 개수를 표현(개념 목업 확인 완료, 2026-09-01).
- ⚠️ **주의**:
  - 목업의 대륙 모양은 정확한 지리 데이터가 아님 — 실제 구현 시 정확한
    벡터 윤곽 데이터(예: Natural Earth, 퍼블릭 도메인)로 교체 필요.
  - 확대/이동이 없는 고정 크기 지도라, 서울-도쿄처럼 가까운 도시는 점과
    라벨이 겹칠 수 있음 — 라벨 겹침 회피 로직이나 지도 옆 목록 병행 표시로
    보완 필요.

### 위치 정보 추론 (GPS 없는 사진 보완)

GPS가 없는 사진이 훨씬 많을 것으로 예상되므로(위 "장소별 분류" 주의사항 참고),
아예 방치하지 않고 최대한 보완하는 방법. **둘 다 AI/ML 불필요** — 정렬·시간차
계산·기존 계획된 유사 중복 클러스터 재사용만으로 충분한 순수 로직.

- **시간 기반 보간**: 같은 스캔 안에서 GPS 있는 사진들을 촬영 시각순으로
  정렬해두고, GPS 없는 사진은 시간상 가장 가까운 GPS 사진의 위치를 가져다
  쓰거나(또는 앞뒤 두 GPS 지점 사이를 선형 보간).
- **유사 사진 클러스터 기반 전파**: "1차 대상: 중복/유사 사진 탐지"에서 계획된
  퍼셉추얼 해시 클러스터를 재사용 — 같은 클러스터 안에 GPS 있는 사진이 하나라도
  있으면 클러스터 전체에 그 위치를 적용. 추가 계산 비용이 거의 없음(이미 계획된
  기능의 부산물).
- ⚠️ **주의사항**:
  - 시간 간격이 너무 벌어지면(비행기 이동 등) 완전히 틀린 위치가 나올 수 있음
    — "N시간 이내"로 보간 상한을 두고, 상한을 넘으면 그냥 "위치 정보 없음"으로
    남긴다.
  - 이렇게 추론한 위치는 실제 GPS 값과 반드시 구분해서 "추정 위치"라고
    표시 — 실측 데이터처럼 보이면 안 됨.

### 사용자가 직접 넣는 위치 정보

기존에도 사용자가 폴더명에 장소를 적어두는 경우가 많다는 점에 착안 — 자동
추론이 실패하거나 틀렸을 때 사용자가 직접 고칠 수 있는 통로.

- **뷰어에서 직접 수정**: 위치 뷰어에서 사진(또는 그룹)을 선택해 위치를 직접
  입력/수정할 수 있게 함.
- **폴더명 힌트**: 사용자가 이미 지어둔 폴더명(예: "제주도", "도쿄여행")이
  도시 데이터셋(위 `reverse_geocoder` 목록)과 매칭되면 "이 폴더명(OO)을
  위치로 쓸까요?" 하고 물어본 뒤 반영.
- ⚠️ **주의사항**:
  - 사용자가 직접 입력/승인한 값은 사진 파일(EXIF)에 다시 쓰지 않고 **앱
    안에서만**(정리 실행 시 목적지 폴더를 정하는 용도로만) 사용한다 — 파일을
    건드리는 순간 "원본 보호" 원칙과 정면으로 충돌하기 때문.
  - 폴더명을 자동으로 신뢰하지 않는다 — "새 폴더", "정리중"처럼 지명과 무관한
    이름도 많으므로, 매칭되더라도 항상 사용자 확인을 거친 뒤에만 반영한다.

## 관련 문서

- [PRD_MVP우선순위.md](PRD_MVP우선순위.md) — P3 섹션, 지금까지 완료된 MVP 범위
- [PicMedic_PRD_v2.md](PicMedic_PRD_v2.md) — 원 PRD, 1.2절에 Phase 2 문제 정의
- [DESIGN.md](DESIGN.md) — 새 화면/카드 만들 때 따를 디자인 패턴
