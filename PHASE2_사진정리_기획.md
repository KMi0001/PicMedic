# Phase 2 — 사진 정리 상세 기획

> PRD_MVP우선순위.md의 P3 "Phase 2 — 사진 정리" (원 PRD 31장: 중복/유사 사진 탐지,
> 날짜·장소별 분류, EXIF 기반 정리, 자동 폴더 구성)를 실제로 만들기 위한 상세
> 기획서.

## ✅ 1차 범위 확정 (2026-08-31)

- **정확 중복만** (SHA-256 파일 내용 해시). 유사 중복(퍼셉추얼 해시)은 다음 단계.
- **별도 "중복 사진" 화면 신설** — 검사 결과 화면의 표/필터에 통합하지 않음.
  다만 완전히 새로운 진입점(홈 화면부터 새 스캔)은 아니고, **같은 검사 세션
  (`ScanSessionWindow`) 안에서 검사 결과 화면 → "중복 파일 보기" 버튼 →
  이 새 화면**으로 이동하는 구조. 같은 스캔에서 이미 계산한 해시를 그대로
  쓰므로 다시 스캔할 필요 없음.
- **찾아서 보여주기까지만** — 삭제/이동 액션은 이번 1차에 넣지 않음. 다음
  단계에서 별도로 설계.

## 왜 이 순서인가

[PicMedic_PRD_v2.md](PicMedic_PRD_v2.md) 1.2절이 PicMedic이 다루는 핵심 문제
목록에서 "동일 사진이 여러 개 존재하는 문제"를 Phase 2의 대표 문제로 명시하고
있음 — 즉 **중복 탐지가 Phase 2의 원래 취지에서 가장 먼저였던 항목**.

## Phase 2의 4개 항목, 실현 가능성 검토

| 항목 | 기술 난이도 | 외부 의존성 | 원칙 충돌 소지 | 1차 추천 |
|---|---|---|---|---|
| 중복/유사 사진 탐지 | 낮음~중간 | `imagehash`(선택) | 없음 | ✅ |
| 날짜별 분류 | 낮음 | 없음(EXIF 이미 읽음) | 낮음(자동 폴더 구성과 묶일 때만) | 2차 |
| 장소별 분류 | 중간~높음 | 역지오코딩(오프라인 DB 또는 인터넷) | "로컬 전용" 원칙과 충돌 가능 | 보류 |
| 자동 폴더 구성 | 낮음(로직) / 설계는 신중 | 없음 | **높음** — "원본 보호" 원칙과 직접 충돌 | 2차 (복사 전용으로) |

**장소별 분류가 보류인 이유**: GPS 좌표(위도/경도)까지는 EXIF에서 바로 뽑을 수
있지만, 이걸 "서울시 강남구"처럼 사람이 읽는 지명으로 바꾸려면 역지오코딩이
필요함 — 인터넷 API를 쓰거나 오프라인 지명 DB를 앱에 내장해야 함. 둘 다 이번
1차 범위에 넣기엔 무거워서, 필요하면 위경도만 노출하는 수준으로 축소하거나
아예 다음 단계로 미룸.

## 1차 대상: 중복/유사 사진 탐지

### 범위

- **정확 중복(바이트 단위 동일 파일)**: 파일 내용 해시(SHA-256) 비교. 구현
  단순, 오탐 없음.
- **유사 중복(리사이즈/약간 편집/다시 저장된 사진)**: 퍼셉추얼 해시(perceptual
  hash) 비교 — `imagehash` 패키지(Pillow 기반, 가벼움) 사용 권장. 임계값 이하
  해밍 거리면 "유사"로 묶음.
- 두 종류를 구분해서 보여줌: "완전 중복 N그룹" / "유사 중복 N그룹".

### 데이터 모델 변경 (`models/`)

- `FileInfo`에 필드 추가: `content_hash: Optional[str]`, `perceptual_hash: Optional[str]`
- `ScanResult`에 중복 그룹 계산 메서드 추가: `duplicate_groups() -> list[list[FileInfo]]`
  (스캔이 끝난 뒤, 이미 채워진 해시들을 기준으로 그룹핑 — 스캔 자체를 다시
  돌 필요 없음)

### 스캔 로직 변경 (`core/scanner.py`, `core/analyzer.py`)

- `analyze_file`이 정상 디코딩에 성공한 파일에 한해 해시 2종을 같이 계산.
  (손상/미지원 파일은 중복 비교 대상에서 자연히 제외됨)
- 해시 계산은 이미 파일을 열어 디코딩하는 김에 같이 하므로 **추가 파일 I/O는
  없음** — 단, HEIC처럼 무거운 형식은 해시 계산 자체도 비용이 있어 체감 스캔
  시간에 영향 줄 수 있음(갭 #8 경험상 미리 염두에 둘 것 — 필요하면 "정확 중복만
  먼저, 유사 중복은 옵션으로 켜기" 식으로 나눔).

### 화면 구조 (확정)

**`gui/duplicate_screen.py` 신설** — 같은 검사 세션(`ScanSessionWindow`) 안의
새 화면. `ResultScreen`에 "중복 파일 보기" 버튼을 추가해서 이동한다(기존
"복구 가능한 파일 보기" 버튼과 같은 자리/스타일). 다시 스캔하지 않고, 이미
계산된 `content_hash`로 `ScanResult.duplicate_groups()`를 호출해 그룹만
보여준다.

- 헤더: 아이콘 + "중복 사진" 제목, 오른쪽 위에 "← 뒤로"(검사 결과 화면으로).
- 상단 요약: `SummaryChip` 재사용 — "중복 그룹" 개수, "중복 파일" 총 개수.
- 그룹 목록: 그룹마다 작은 카드로, 그룹 안 파일들(파일명 + 경로 + 크기) 나열.
  삭제/이동 버튼 없음(1차 범위 밖) — 순수 확인용.
- 중복이 하나도 없으면 "중복된 파일이 없습니다" 안내만 표시.

### 2차(이번엔 범위 밖, 다음 단계): 날짜 기반 자동 폴더 구성

- EXIF `DateTimeOriginal` 기준 "YYYY/MM" 폴더로 정리.
- **원본 보호 원칙 때문에 기본은 "복사"** — 새 폴더 구조에 복사본을 만들고,
  원본은 그대로 둠. "이동"은 명시적으로 켜야 하는 옵션 + 강한 경고 문구.
- 이미 복구/변환 흐름에 있는 `_confirm_dialog`/진행 팝업 패턴을 그대로 재사용
  가능(이동/복사 대상 파일 수가 많으면 배치 작업이라 진행률 팝업 필요).

## 관련 문서

- [PRD_MVP우선순위.md](PRD_MVP우선순위.md) — P3 섹션, 지금까지 완료된 MVP 범위
- [PicMedic_PRD_v2.md](PicMedic_PRD_v2.md) — 원 PRD, 1.2절에 Phase 2 문제 정의
- [DESIGN.md](DESIGN.md) — 새 화면/카드 만들 때 따를 디자인 패턴
