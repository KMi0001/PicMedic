# CHANGELOG

PicMedic 버전별 릴리즈 노트. 새 버전을 커밋/푸시할 때마다 맨 위에 새 섹션을 추가한다.

## v1.2.0 — 2026-08-31

### 🐛 버그 수정
- 정상 파일이 섞인 채로 일괄 복구 시 처리 방식 정리 (갭 #11) — 확장자 복원 모드는
  이미 정상인 파일을 파일 I/O 없이 건너뛰도록 수정(결과에 "건너뜀"으로 별도 표시)
- 검사 결과 화면 "전체 선택 → Medic!"으로 들어갈 때 복구 모드가 항상 "형식 변환"으로
  잡히던 문제 수정 — 복원 가능한 파일이 있으면 더 안전한 "확장자 복원"이 기본값이 됨

### ✨ UI/UX 전면 개선
- **팝업을 앱 테마에 맞춰 재설계**: 네이티브 `QMessageBox` 대신 카드형 커스텀 다이얼로그
  사용 (정상 파일 건너뛰기 확인 팝업에 취소/확인 버튼 추가 등)
- **사진 복구/변환 화면**: 카드 안 섹션 제목(복구 방식/저장 위치 등) 강조, 선택 파일을
  상태별로 보여주는 요약 카드 추가(고정폭 + 중앙정렬), 드롭다운(콤보박스)을 민트색
  버튼형 화살표로 스타일링
- **진행 상황을 모달 팝업으로 전환**: 복구/변환 중에는 별도 팝업이 뜨고, 그동안 다른
  설정을 건드리거나 뒤로 갈 수 없게 막음. **취소 버튼** 추가 — 취소하면 그때까지 만든
  결과물을 유지할지 삭제할지 선택 가능(원본 파일은 항상 보존)
- **복구 완료 화면**: "OO 파일 보기" 버튼들을 없애고, 상단 상태 요약 카드를 직접 눌러
  해당 파일 목록을 확인하도록 변경
- **검사 결과 화면**: "다시 검사" 버튼을 실제 동작(홈 화면 복귀)에 맞춰 "홈" + 집
  아이콘으로 교체, 제목 옆에 돋보기 아이콘 추가, 상태 요약 카드를 눌러 표를 바로
  필터링할 수 있게 함
- **최근 검사 팝업**: 텍스트 나열 방식에서 상태 아이콘 + 카드형 요약으로 리디자인.
  이후 복구/변환까지 했다면 처리 개수와 저장 폴더 바로가기도 함께 표시(폴더가
  이동/삭제됐으면 조용히 무시하지 않고 안내 문구 표시)
- 이모지 대신 항상 `QPainter`로 직접 그린 벡터 아이콘 사용(Windows/macOS 렌더링 차이 회피)
- `DESIGN.md` 신설 — 색상 팔레트, 아이콘 시스템, 팝업/카드 컴포넌트 패턴을 문서화해
  앞으로 UI를 건드릴 때 참고하도록 함

### 🧪 테스트
- `tests/test_converter.py` 46/46, `tests/test_result_screen.py` 9/9 통과 확인
- 실제 배치(최대 200개 파일)로 진행 취소 → 결과 유지/삭제 두 경로 모두 동작 확인

---

## v1.1.0 — 2026-08-30

### 🐛 버그 수정
- 심볼릭 링크 순환 스캔 무한루프 — iCloud/OneDrive처럼 자기 자신을 가리키는 폴더가 있으면
  스캔이 끝없이 돌던 문제, 그 상태에서 취소 버튼이 안 먹히던 문제 수정
- 부분 손상 파일 복구 실패 — "부분 손상"으로 판정된 파일을 변환하면 항상 실패하던 문제 수정
- 예외 메시지 오류 — 권한 없음/다른 프로그램 사용 중/저장 공간 부족 상황에서 정확한 안내
  문구로 수정, 저장 공간 부족 시 배치 작업이 올바르게 중단되도록 수정
- 복구 불가능 파일 체크박스 — 완전히 손상돼 복구가 불가능한 파일도 체크박스가 활성화돼
  있던 문제 → 비활성화 + "복구할 수 없는 파일입니다" 툴팁 추가
- 홈 화면 렌더링 버그 — 헤더 정렬/간격, 선택 카드 아이콘(폴더 이모지 → 벡터 아이콘),
  최근 검사 카드의 배경색·모서리·구분선 렌더링 오류 다수 수정

### ✨ 새 기능 / 개선
- 검사 결과 검색 범위 확장 (파일명 → 파일명+확장자+실제 형식)
- 검사 결과 테이블에 "크기"/"수정일" 컬럼 추가 + 정렬 지원
- 형식 변환 시 화질(고화질/보통/저용량) 선택 기능 추가
- GIF/TIFF/BMP 스캔·변환 지원 추가
- macOS 실행 파일 빌드 지원, macOS 전용 파일(.DS_Store 등) 및 심볼릭 링크 순환 스캔 회피 처리
- 홈 화면 UX 개선 (선택 카드 통합, 최근 검사 카드형 목록 디자인)
- 상세 화면 레이아웃 가로 → 세로 배치로 변경
- 정상 파일은 "복구" 대신 바로 "변환"만 하도록 흐름 단순화 (불필요한 선택지 제거)
- 버튼 문구 통일: "선택 항목 복구" → "Medic!"

### 🧪 테스트
- 자동화 테스트 95~99개 전부 통과 (신규 `test_scanner.py`, `test_result_screen.py` 추가)

---

## v1.0.0 — MVP

PRD 기준 P0~P2 범위 전체 구현 완료. 자세한 내용은 `PRD_MVP우선순위.md` 참고.
