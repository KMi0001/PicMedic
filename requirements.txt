Pillow>=10.0
pillow-heif>=0.15
PySide6>=6.6
ImageHash>=4.3

# core/face_restorer.py ("얼굴 복원") — RestoreFormer++ 추론에 필요.
# basicsr/RestoreFormer 자체는 vendor/에서 온다(pip 패키지가 아님, vendor/*/NOTICE 참고).
torch>=2.0
torchvision>=0.15
facexlib>=0.3

# core/photo_category.py ("사진 진단"의 카테고리 판단) — CLIP(open_clip) 추론에 필요.
open_clip_torch>=2.20

# core/geocoder.py ("도시별 정리") — GPS 좌표 -> 도시명 매칭. 오프라인
# (assets/geonames_cities1000.csv 내장, 인터넷 호출 없음), scipy(cKDTree)로 직접
# 구현. 2026-09-11: reverse_geocoder 패키지(LGPL — 상업 배포 시 PyInstaller 정적
# 번들링과 라이선스 조건이 맞물리는 부분이 있어 법률 검토가 필요했음, 대신
# 자체 구현으로 교체)를 걷어내면서, 그 패키지가 간접적으로 끌어오던 scipy를
# 직접 명시해야 했다(안 그러면 fresh install에서 core/geocoder.py가 import
# 에러로 깨짐 — 실제로 이 실수를 했다가 잡음).
scipy>=1.7
