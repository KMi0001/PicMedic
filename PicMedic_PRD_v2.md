# PicMedic
사진 파일 진단 · 복구 · 정리 프로그램
Product Requirements Document (PRD) — **v2 개정판 (2026-08-30)**

> 원본: `PicMedic Product Requirements.pdf` (v1). 이 문서는 v1의 구조와 내용을 그대로 유지하면서, 실제 구현 과정에서 확정되거나 바뀐 사양을 반영한 개정판입니다.
> 변경된 절에는 **[개정]** 표시를, 이번에 새로 추가된 내용에는 **[신규]** 표시를 달았습니다. 표시가 없는 절은 v1과 동일합니다.
> 세부 구현 상태(완료/부분완료/미구현) 추적은 [PRD_MVP우선순위.md](PRD_MVP우선순위.md)를 참고하세요. 이 문서는 "무엇을 만들기로 했는가"를, 그 문서는 "얼마나 만들었는가"를 다룹니다.

---

## 1. 프로젝트 개요

### 1.1 프로젝트명
PicMedic

손상되거나 잘못 인식된 사진 파일을 진단하고, 가능한 경우 복구하여 사용자가 안전하게 사진을 되살릴 수 있도록 돕는 Windows용 사진 관리·복구 프로그램.

### 1.2 프로젝트 목적
스마트폰, 클라우드, 외장 저장장치 등의 백업 과정에서 발생할 수 있는 사진 파일 문제를 사용자가 직접 확인하고 해결할 수 있도록 한다.

특히 다음과 같은 문제를 주요 대상으로 한다.
- HEIC 사진이 JPEG로 잘못 확장되거나 인식되는 문제
- 파일 확장자와 실제 파일 형식이 다른 문제
- 이미지 파일 일부가 손상된 문제
- 사진은 존재하지만 Windows에서 열리지 않는 문제
- 동일 사진이 여러 개 존재하는 문제 (Phase 2)
- 대량의 사진을 하나씩 확인하기 어려운 문제

PicMedic은 사용자가 파일 내부 구조를 직접 분석하지 않아도 "이 사진이 어떤 상태인지 → 무엇을 할 수 있는지 → 어떻게 복구할 것인지"를 단계적으로 안내하는 것을 목표로 한다.

---

## 2. 문제 정의

### 2.1 사용자 문제
사진 백업 과정에서 파일의 확장자나 메타데이터가 변경되면 사용자는 다음과 같은 상황을 경험할 수 있다.
- "파일은 있는데 사진이 안 열려요."
- "JPEG라고 되어 있는데 실제로는 HEIC인 것 같아요."
- "사진이 수천 장이라 하나씩 확인할 수 없어요."

기존 이미지 뷰어는 정상적인 이미지 파일을 보여주는 데 초점이 맞춰져 있어 파일 자체가 비정상적인 경우 원인을 알려주지 못하는 경우가 많다.

### 2.2 해결 방향
PicMedic은 이미지 파일을 단순히 열어보는 것이 아니라, **파일 분석 → 문제 진단 → 복구 가능성 판단 → 복구 → 결과 검증**의 흐름으로 문제를 해결한다.

---

## 3. 핵심 가치

- **Diagnose** — 파일의 확장자와 실제 파일 구조를 분석한다.
- **Recover** — 복구 가능한 파일은 원본을 보존하면서 새로운 파일로 복구한다.
- **Organize** — 대량의 사진을 상태별로 분류하고 관리한다.
- **Protect** — 원본 파일을 직접 수정하지 않고 안전한 복구 방식을 우선한다.

---

## 4. 타깃 사용자

**Primary User** — 사진 백업 과정에서 파일 문제가 발생한 일반 사용자. 사진 파일 구조를 잘 모르고, Windows PC를 사용하며, 파일은 존재하지만 사진이 정상적으로 열리지 않는 문제를 경험함.

**Secondary User** — 대량의 사진을 관리하는 사용자. 수천~수만 장의 사진 보유, 중복 사진 다수, 오래된 백업 정리 필요, 사진 상태를 일괄적으로 검사하고 싶음.

---

## 5. 제품 목표

**MVP 목표**: 사용자가 사진 폴더를 선택하면 PicMedic이 자동으로 파일을 분석하여 다음 정보를 제공한다.
1. 파일 확장자 2. 실제 파일 형식 3. 파일 정상 여부 4. 손상 여부 5. 복구 가능 여부 6. 예상 복구 방법 7. 복구 결과

**MVP 성공 기준**: 사용자는 별도의 전문 지식 없이 "내 사진이 왜 안 열리는가?", "복구할 수 있는가?", "복구하면 어떤 파일이 만들어지는가?"에 답을 얻을 수 있어야 한다.

---

## 6. 핵심 기능

### 6.1 폴더/파일 선택 **[개정]**
사용자가 검사할 파일 또는 폴더를 선택한다.

지원 방식
- 단일 파일 선택
- **여러 파일 동시 선택** [신규] — 파일 선택 다이얼로그에서 다중 선택(Ctrl/Shift), 드래그 앤 드롭으로 여러 항목을 한 번에 끌어놓기 모두 지원. 서로 다른 경로로 같은 파일이 중복 포함되면 한 번만 검사한다.
- 폴더 선택
- 하위 폴더 포함 검사
- 드래그 앤 드롭
- **폴더/파일 선택 다이얼로그는 마지막으로 사용한 위치에서 열리며, 기록이 없으면 시스템 "사진" 폴더에서 시작한다** [신규]

지원 확장자

MVP:
- .jpg / .jpeg
- .png
- .heic / .heif
- **.webp** [개정] — 원래 "향후 지원" 예정이었으나, 복구 시 변환 대상 형식으로 필요해져 정식 지원 형식으로 승격함. 스캔 대상 파일로도 정상 인식된다.

향후:
- .gif
- .tiff
- .bmp
- RAW

### 6.2 파일 분석 기능
PicMedic은 파일 확장자만 확인하지 않고 파일 내부 Signature / Magic Number 및 이미지 디코더를 이용하여 실제 파일 형식을 판별한다. (분석 항목 표는 v1과 동일: File Name, Extension, Detected Format, File Size, Dimensions, MIME Type, Metadata, Readable, Corruption, Recovery)

### 6.3 파일 상태 분류 **[개정]**
각 파일은 다음 상태 중 하나로 분류한다.

| 상태 | 의미 |
|---|---|
| 🟢 정상 | 확장자와 실제 형식이 일치하며 정상적으로 이미지가 읽힘 |
| 🟡 형식 불일치 | 예: `photo.jpg`인데 실제 내부 형식은 HEIC → 확장자 변경/변환 가능성 있음 |
| 🟠 부분 손상 | 파일의 일부 데이터가 손상되었지만 일부/전체를 읽을 수 있음 |
| 🔴 손상 | 이미지 디코딩에 실패하여 정상적으로 표시할 수 없음 |
| ⚪ 지원되지 않는 형식 | PicMedic이 현재 지원하지 않는 파일 |
| ⚫ 이미지가 아닌 파일 | 확장자는 이미지처럼 보이지만 실제 이미지 파일이 아닌 경우 |
| 🔵 **복구 완료** [신규] | 형식 불일치·부분 손상 파일의 복구가 성공적으로 끝나면 자동으로 이 상태로 전환됨. 원래 "정상"이던 파일을 형식 변환(6.5 참고)한 경우에는 이 상태로 바뀌지 않는다 — 고장난 걸 고친 게 아니라 단순 변환이기 때문 |

### 6.4 HEIC → JPEG 문제 진단
(v1과 동일) 파일명은 JPG인데 실제 형식이 HEIC인 경우를 자동으로 "형식 불일치"로 분류하고 사용자에게 안내한다.

### 6.5 복구 기능

#### 6.5.1 원본 보호
PicMedic은 기본적으로 원본 파일을 직접 수정하지 않는다. 복구 결과는 항상 별도 폴더(`Recovered/`)에 새 파일로 생성되며, 원본은 그대로 유지된다.

**[개정]** Recovery 화면의 "원본 파일 유지" 체크박스는 삭제했다. 이 정책은 사용자가 끌 수 없는 항상-참(always-true) 규칙인데 토글 UI로 노출하면 마치 끌 수 있는 것처럼 오해를 줄 수 있어서, 대신 "원본 파일은 항상 그대로 보존되며, 복구 결과는 별도 폴더에 새 파일로 저장됩니다"라는 안내 문구로 대체했다.

#### 6.5.2 복구 가능성 표시
(v1과 동일) 복구 가능 / 부분 복구 가능 / 복구 불가능 3단계로 표시.

### 6.6 이미지 변환 **[개정]**
v1에서는 "JPEG 변환"으로 출력 형식이 고정되어 있었으나, **출력 형식을 사용자가 선택**하도록 일반화했다.

지원 변환 형식
- **JPEG** (기본값)
- **PNG** — v1 예시 표(HEIC→JPEG, HEIF→JPEG, PNG→JPEG, JPEG→PNG)에 있었으나 미구현 상태였던 것을 구현함
- **WEBP** [신규, PRD 원 범위 밖] — 웹 업로드 등 용도로 요청되어 추가. JPEG보다 용량이 작고 화질을 잘 유지하며, 알파(투명) 채널도 지원함

변환 규칙
- 알파(투명) 채널을 지원하는 형식(PNG/WEBP)은 투명도를 그대로 유지
- 지원하지 않는 형식(JPEG)으로 저장할 때는 자동으로 RGB로 눌러서 저장 (PNG의 완전 투명 영역이 사라질 수 있음 — 사용자가 알 필요는 없지만 개발 시 유의)
- 품질(quality)은 내부적으로 90으로 고정되어 있으며, 사용자가 조절하는 UI는 아직 없음 (37장 개선 제안 참고)

변환 옵션
- 출력 형식 (JPEG/PNG/WEBP 드롭다운) [신규]
- 저장 위치
- **파일명에 추가할 문구** [신규] — 기본값 `recovered`, 사용자가 자유롭게 수정 가능 (예: `IMG_1234_복구완료.jpg`). 파일명에 쓸 수 없는 특수문자는 자동으로 제거되고, 비워두면 기본값으로 대체됨

기본값: 원본 유지 + 별도 폴더 저장

### 6.7 일괄 처리 **[개정]**
대량 파일을 한 번에 처리할 수 있어야 한다.

- 검사 결과 목록의 **각 행 왼쪽에 체크박스**가 있어 개별 선택 가능
- **행을 클릭해 선택해도 체크박스가 같이 체크되고, 체크박스를 체크해도 행이 하이라이트된다** [신규] — 두 선택 방식이 완전히 동기화되어 있음
- **테이블 헤더의 체크박스로 전체 선택/해제** [신규] — 필터로 "복구 가능한 파일만" 등을 먼저 좁힌 뒤 헤더 체크박스로 전체 선택하면 사실상 "전체 복구"와 동일하게 동작
- 필터
  - 전체 / 정상 / 형식 불일치 / 부분 손상 / 손상 / **복구 완료** [신규] / 복구 가능 / 지원안함·오류
- **컬럼 클릭 정렬** [신규] — 상태/파일명/실제 형식/확장자 컬럼 클릭 시 오름차순·내림차순 정렬 (파일 크기·날짜 정렬은 해당 컬럼 자체가 아직 없어 미지원)

### 6.8 미리보기
(v1과 동일)

### 6.9 검색 및 정렬
- 검색: 파일명만 지원 (확장자/실제 형식 검색은 아직 미구현)
- 정렬: 6.7 참고

### 6.10 복구 결과 검증
(v1과 동일 원칙 유지) 복구 작업이 끝난 후 생성된 파일을 다시 검사하여, 실제로 다시 읽을 수 있는지 검증한다. "파일 생성 완료"만으로 성공을 판단하지 않는다.

---

## 7. 화면 구성 **[개정]**

### Screen 01 — Home **[개정]**
```
┌──────────────────────────────────────┐
│ PicMedic                              │
│ 사진을 검사하고 복구하세요             │
│                                        │
│ ┌───────────┐  ┌───────────┐         │
│ │ 파일 선택  │  │ 폴더 선택  │         │
│ └───────────┘  └───────────┘         │
│                                        │
│ 파일을 여기에 끌어놓으세요 (여러 개 OK) │
│                                        │
│ 최근 검사                              │
│ • Photos_2026        ✓ 완료 (128장)   │
│ • 3개 파일 선택       ⚠ 중단됨 (2/3)  │
└──────────────────────────────────────┘
```
- **"파일 선택" 버튼 추가** [신규] — 기존엔 "폴더 선택"만 있었음. 이미지 확장자로 필터링된 다중 선택 다이얼로그
- **최근 검사 항목에 완료/중단 상태와 처리 개수 표시** [신규]
- **최근 검사 항목 더블클릭 시 재스캔하지 않고, 그때 결과 요약을 팝업으로 바로 보여줌** [신규] (정상/형식불일치/손상 등 개수 breakdown + "다시 검사" 버튼)
- 검증된 파일이 0개인 스캔(빈 폴더 등)은 최근 검사 목록에 남기지 않음 [신규]

### Screen 02 — Scanning **[개정]**
```
┌──────────────────────────────────────┐
│ 사진 검사 중...                        │
│ ███████████████░░░░░ 72%              │
│ 7,245 / 10,032                        │
│ 현재 검사: IMG_7245.jpg                │
│ [ 취소 ]                               │
└──────────────────────────────────────┘
```
- 표시 정보는 v1과 동일 (전체 파일 수, 현재 처리 파일, 진행률, 경과 시간, 예상 남은 시간)
- **취소 동작 변경** [개정] — v1 구현 초기에는 취소 시 그때까지의 결과를 버리고 홈 화면으로 돌아갔으나, **이제는 검사된 파일이 하나라도 있으면 Scan Result 화면으로 이동해 그 결과를 그대로 보여준다.** 완전히 아무것도 검사하지 못한 채 취소한 경우에만 홈으로 돌아간다.
- **"이어서 검사"** [신규] — 취소된 스캔은 Scan Result 화면에 "⚠ 검사가 중단되어 N/M개 파일까지만 검사되었습니다" 배너와 함께 "이어서 검사" 버튼이 뜬다. 누르면 나머지 파일만 마저 스캔해서 이전 결과와 자동으로 합친다. 여러 번 중단해도 이어붙일 수 있다. **단, 같은 앱 실행 세션 안에서만 가능** — 앱을 껐다 켜면 다시 처음부터 스캔해야 한다.

### Screen 03 — Scan Result **[개정]**
```
┌──────────────────────────────────────┐
│ 검사 결과                    [다시검사]│
│ ⚠ 검사가 중단되어...      [이어서검사] │
│                                        │
│ 총파일 정상 형식불일치 손상 복구완료   │
│ 10032 9821    512      48    128      │
│                                        │
│ [복구 가능한 파일 보기]  검색🔍 필터▾ │
│                                        │
│ ☑ 상태     파일명       실제형식 확장자│
│ ☑ 정상    IMG001.jpg   JPEG     .jpg  │
│ ☐ ⚠ 형식불일치 IMG002.jpg HEIC  .jpg  │
│                                        │
│ 2개 파일 선택됨          [선택 항목 복구]│
└──────────────────────────────────────┘
```
- 요약 카드에 **"복구 완료" 칩 추가** [신규]
- 검사가 중단된 결과일 경우 상단에 배너 + "이어서 검사" 버튼 [신규]
- 목록 왼쪽에 **체크박스 칼럼**, 헤더에 **전체선택/해제 체크박스** [신규]
- 컬럼 클릭 정렬 [신규]

### Screen 04 — File Detail
(v1과 동일한 레이아웃 유지. 복구 버튼 두 개는 6.6의 일반화된 변환 옵션으로 연결됨)

### Screen 05 — Recovery **[개정]**
```
┌──────────────────────────────────────┐
│ ← 뒤로                                │
│ 사진 변환                     ← 모드에 따라 "사진 복구"/"사진 변환" 자동 전환
│                                        │
│ 선택 파일: 512개                       │
│                                        │
│ 복구 방식                              │
│ ○ 확장자 복원                          │
│ ● 형식 변환      [JPEG ▾]  ← 출력 형식 드롭다운
│                                        │
│ 저장 위치                              │
│ D:\Recovered\                          │
│                                        │
│ 파일명에 추가할 문구                    │
│ [recovered________]                    │
│                                        │
│ 원본 파일은 항상 그대로 보존되며...     │
│ ☑ 복구 후 파일 검증                    │
│                                        │
│ [ Medic! ]                             │
└──────────────────────────────────────┘
```
- 화면 제목이 라디오 선택에 따라 "사진 복구" ↔ "사진 변환"으로 자동 전환 [신규] — "확장자 복원"은 진짜 복구지만 "형식 변환"은 정상 파일에도 쓸 수 있는 순수 변환 기능이라 용어를 구분함
- 시작 버튼 라벨을 **"Medic!"** 으로 통일 [신규] — 복구/변환 어느 쪽이든 "고친다"는 느낌을 주는 앱 고유의 액션 버튼
- "형식 변환" 선택 시에만 출력 형식 드롭다운(JPEG/PNG/WEBP) 활성화 [신규]
- "원본 파일 유지" 체크박스 삭제, 안내 문구로 대체 [신규]
- 파일명 문구 직접 입력 필드 추가 [신규]

### Screen 06 — Recovery Result
(v1과 동일)

---

## 8. 기능 요구사항 **[개정]**

### FR-001 파일 선택
- [x] 파일 선택 가능 (**단일 + 다중** [개정])
- [x] 폴더 선택 가능
- [x] 하위 폴더 탐색 가능
- [x] 드래그 앤 드롭 가능 (**단일 + 다중** [개정])
- [x] 지원하지 않는 파일은 자동 필터링

### FR-002 파일 형식 탐지 / FR-003 이미지 유효성 검사
(v1과 동일, 변경 없음)

### FR-004 복구
- [x] 원본 유지
- [x] 출력 폴더 지정
- [x] **출력 형식 지정** [신규] (JPEG/PNG/WEBP)
- [x] 동일 파일명 충돌 처리
- [x] **복구 후 파일명 문구 사용자 지정** [신규]
- [x] 복구 후 재검증

### FR-005 일괄 복구
- [x] 다중 선택 (**체크박스 + 행 클릭 동기화** [개정])
- [x] 전체 복구 (**헤더 체크박스로 구현** [개정])
- [x] 상태별 복구
- [x] 진행률 표시
- [x] 실패 파일 별도 표시

### FR-006 로그
(v1과 동일) 각 작업 결과를 `logs/picmedic_log.jsonl`에 JSON Lines로 기록. 스캔 로그와 복구 로그(**변환 형식 정보 포함** [신규])를 모두 남긴다.

---

## 9. 예외 처리

(v1과 동일한 9개 시나리오 정의 유지. **23.1 권한 없음, 23.3 파일 잠금, 23.5 저장 공간 부족 세 가지는 PRD가 지정한 정확한 안내 문구·"작업 중단" 동작이 아직 일반 예외 메시지로만 처리되고 있음 — 37장 개선 제안 1번 참고.** 나머지(23.2/23.4/23.6/23.7/23.8)는 요구사항대로 구현됨.)

---

## 10. 비기능 요구사항
(v1과 동일, 변경 없음 — 성능/안정성/사용성/안전성 모두 충족. 사용성 중 오류 메시지 항목만 9장의 갭과 연결됨)

---

## 11. 기술 구성 **[개정]**

- Application: Python / GUI: PySide6 / Image Processing: Pillow + pillow-heif
- File Analysis: 파일 Signature/Magic Number 분석, MIME Type 분석, 이미지 디코딩 검증
- **배포: PyInstaller로 단일 실행 파일(exe) 빌드 지원** [신규] — `dist/PicMedic.exe`, 자세한 빌드 방법은 `README.md` 참고

Architecture (v1과 동일)
```
PySide6 GUI
    │
    ▼
Application Core
├── Scanner
├── Analyzer
├── Recovery Manager (converter.py에 통합됨)
├── Converter
├── Validator (analyzer.py에 통합됨)
└── Logger
    │
Pillow / HEIC / File System
```

---

## 12. 데이터 모델 **[개정]**

### FileInfo
(v1 필드 유지) path, filename, extension, detected_format, mime_type, file_size, width, height, metadata, status, readable, is_mismatched, corruption_ratio, recoverable, error_message

- `status`에 **`RECOVERED`("복구_완료")** 값 추가 [신규]

### ScanResult
(v1 필드 유지) total, normal, mismatch, partial_corruption, corrupted, unsupported, files[]

- **`recovered`** 카운터 추가 [신규]
- **`not_an_image`** 카운터 추가 [신규] (v1엔 명시 안 됐던 상태지만 8장 상태 분류에 있어서 집계 필드로 추가함)
- **`mark_recovered(info)`** — 복구 성공 시 파일 상태를 갱신하고 집계를 조정 [신규]
- **`merge(other)`** — 이어서 검사한 결과를 하나로 합침 [신규]

---

## 13. MVP 범위 **[개정]**

반드시 구현 — **전부 완료**
- 파일/폴더 선택 (다중 선택 포함)
- 폴더 스캔, 실제 파일 형식 탐지, 확장자 불일치 탐지, 이미지 디코딩 검사
- 정상/손상/**복구완료** 분류
- HEIC 확장자 복구, HEIC→JPEG 변환 (**+ PNG/WEBP 변환** 추가)
- 일괄 처리, 진행률 표시, 결과 화면
- 원본 보호, 복구 후 검증
- 오류 처리 (일부 문구 미세 조정 필요 — 9장 참고)
- 로그

---

## 14. MVP에서 제외 / 15. 향후 확장 / 16. AI 기능 확장 방향
(v1과 동일, 변경 없음)

---

## 17. KPI **[개정]**
(v1과 동일한 5개 지표: 진단 정확도, 복구 성공률, 사용자 작업 완료율, 오류율, 처리 속도)

- **`logs/picmedic_log.jsonl`에 스캔·복구 결과가 모두 기록되므로, 로그를 집계하는 스크립트만 추가하면 이 지표들을 실제로 산출할 수 있는 상태다** [신규] — 별도 대시보드는 아직 없음

## 18. 품질 검증 **[개정]**
- 테스트 스위트: `test_detector.py`(10) + `test_analyzer.py`(12) + `test_converter.py`(19) + `test_logger.py`(14) = **55개, 전부 PASS**
- `test_converter.py`에 PNG 알파 채널 유지, WEBP 변환, 미지원 형식 요청 시 실패 처리 케이스 추가됨 [신규]

## 19. 개발 우선순위
P0/P1/P2 — **완료** (세부 사항은 `PRD_MVP우선순위.md` 참고)
P3 — 미착수 (Phase 2 이후, v1과 동일)

## 20. 최종 제품 정의
(v1과 동일) Product Principle 4가지(Diagnose before Recovery / Protect the Original / Verify the Result / Fail Safely) 모두 실제 구현에 반영되어 있음.

---

## 37. 개선 제안 [신규 장]

이번 구현 과정에서 발견했거나, 다음 단계로 고려해볼 만한 항목들.

### 37.1 예외 메시지 PRD 문구 정합화 (우선순위 높음)
9장에서 언급한 3가지 — 권한 없음, 파일 잠금, 저장 공간 부족 — 은 지금 일반적인 예외 메시지로만 처리되고 있다. PRD가 지정한 정확한 안내 문구로 바꾸고, 특히 저장 공간 부족 시에는 배치 작업을 실제로 중단하도록 고쳐야 한다 (지금은 중단 없이 파일마다 계속 실패 처리됨).

### 37.2 검색 범위 확장
지금은 파일명만 검색 대상이다. 확장자·실제 형식까지 검색되면 "HEIC로 검색해서 형식 불일치 파일만 모아보기" 같은 사용이 가능해진다.

### 37.3 파일 크기 / 날짜 컬럼 및 정렬
현재 정렬은 이미 표시 중인 컬럼(상태/파일명/실제형식/확장자)에만 적용된다. PRD가 원래 요구한 "파일 크기순", "생성/수정 날짜순" 정렬을 하려면 테이블에 해당 컬럼 자체를 먼저 추가해야 한다.

### 37.4 변환 품질(Quality) 조절 UI
JPEG/WEBP 변환 품질이 내부적으로 90으로 고정되어 있다. 저장 용량을 더 줄이고 싶은 사용자를 위해 슬라이더나 프리셋(고화질/보통/저용량)을 Recovery 화면에 노출할 수 있다.

### 37.5 "이어서 검사"의 세션 간 유지
지금은 앱을 끄면 이어서 검사 정보가 사라진다. 세션을 넘어서도 유지하려면 스캔 결과 전체(파일 수천 개분의 FileInfo)를 디스크에 저장해야 하는데, 저장 용량과 "그 사이 파일이 옮겨지거나 삭제됐을 때 결과가 실제와 안 맞을 수 있다"는 위험이 있어 신중히 설계해야 한다.

### 37.6 추가 포맷 지원 (GIF/TIFF/BMP)
WEBP를 추가한 것과 같은 패턴으로 GIF/TIFF/BMP도 스캔 대상 및 변환 대상 형식으로 확장할 수 있다. PRD 6장 "향후" 목록에 이미 있던 항목들이다.

### 37.7 Phase 2 착수 시점
MVP가 사실상 완결된 상태이므로, 다음 확장 단계로 PRD 31장의 Phase 2(중복/유사 사진 탐지, 날짜·장소별 정리)를 언제 시작할지 논의할 시점이 되었다.

### 37.8 [버그] 부분 손상 파일의 복구가 실제로는 항상 실패함
`test_samples/09_손상_부분손상` 샘플로 실제 복구를 시도해보니, 상태·복구가능성은 "부분_손상 / 부분_복구_가능"으로 정확히 표시되는데 실제 변환은 100% 실패한다 (`image file is truncated` 오류).

원인: `core/analyzer.py`는 파일을 검사할 때 Pillow의 "잘린 이미지도 읽을 수 있는 만큼 읽기" 모드(`ImageFile.LOAD_TRUNCATED_IMAGES = True`)를 켜고 디코딩하지만, `core/converter.py::convert_to_format`은 실제 복구(변환) 시 이 모드를 켜지 않는다. 그래서 분석 단계에서는 "일부라도 읽힌다"고 판단해놓고, 정작 복구를 실행하면 Pillow가 기본 설정(엄격 모드)으로 다시 열려다 실패한다.

PRD가 말하는 "부분 복구 가능: 일부 데이터가 손상되어 결과가 완전하지 않을 수 있습니다"는 최소한 시도는 되고 결과물이 나오되 불완전할 수 있다는 뜻인데, 지금은 시도 자체가 항상 실패로 끝난다. **분석 로직과 동일하게 변환 시에도 truncated-image 모드를 켜도록 고쳐야 한다.**

### 37.9 완전 손상 파일에 대한 UI 강조 필요
"손상"(완전 손상, `NOT_RECOVERABLE`) 상태 파일은 애초에 복구가 불가능한 게 설계상 맞는 동작이다 (파일 구조 자체가 남아있지 않아 복구할 대상이 없음 — 실제로 지금 복구 가능한 건 사실상 "형식 불일치"뿐이다). 문제는 지금 검사 결과 목록에서 이런 파일도 다른 파일과 똑같이 체크박스가 활성화되어 있어서, 사용자가 선택해서 "복구"를 시도해볼 수 있다는 점이다 (당연히 실패로 끝남).

개선 방향: 완전 손상(`NOT_RECOVERABLE`) 파일은 목록에서 체크박스를 비활성화하거나, 상세 화면처럼 "복구할 수 없는 파일입니다"라는 문구를 더 눈에 띄게 강조해서 애초에 시도조차 하지 않도록 안내하는 게 좋겠다.

### 37.10 다크모드 토글
지금 색상 구조(`gui/theme.py`의 `COLORS` 딕셔너리를 각 화면 생성 시점에 인라인으로 박아 넣는 방식)에서는 앱이 켜져 있는 도중 실시간으로 테마를 바꾸는 토글을 만들려면 화면 코드 전반의 색상 지정 방식을 정리하는 리팩토링이 필요하다 (작업량 큼). 대신 "토글을 누르면 설정만 저장되고, 다음 실행 시 그 테마로 시작"하는 방식이면 지금 구조에서도 간단하고 안전하게 구현 가능하다. 실시간 전환이 꼭 필요한지, 재시작 적용으로 충분한지부터 정해야 한다.

### 37.11 투명(반투명) 테마
창 전체를 유리처럼 투명하게 만들려면 Windows 기본 타이틀바를 없애고 최소화/최대화/닫기·드래그 이동까지 직접 구현해야 해서 작업 범위가 크고 네이티브 느낌도 떨어질 수 있다. 대신 창은 그대로 두고 카드/패널 배경만 반투명("frosted glass" 느낌)하게 하는 건 지금 구조에서 큰 작업 없이 가능하다.

### 37.12 [신규 아이디어] 프로그램 진입 시 별도의 시작 화면
지금은 실행하면 바로 Home 화면(폴더/파일 선택)이 뜨는데, 향후에는 그 앞에 별도의 시작 화면(스플래시·런처 성격)을 두는 방안을 고려 중. 테마 토글(37.10)이나 최근 검사 요약처럼 앱 전역에 걸친 설정/진입점을 여기에 모아둘 수 있음. 아직 구체적인 설계 전이며, 착수 시점은 별도로 논의.

### 37.13 [시도했다가 되돌림] 검사 결과 목록에 썸네일 미리보기
목록의 모든 행에 작은 미리보기 이미지를 넣어봤는데(화면에 보이는 행만 백그라운드 스레드로 로드하는 방식이라 속도 문제는 없었음), 막상 넣어보니 목록이 시각적으로 지저분해 보인다는 피드백으로 되돌림.

대안으로 논의된 방향: 모든 행에 상시 노출하는 대신, **행을 선택했을 때만** 미리보기가 보이는 방식(예: 목록 하단이나 옆에 작게 표시). 상세 화면(Screen 04)의 미리보기와 겹치는 부분이 있어서, 새로 만들지 상세 화면 진입을 더 쉽게 하는 쪽으로 갈지부터 정하면 좋을 것 같다.
