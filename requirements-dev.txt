# 개발/테스트 전용 의존성. 앱 실행에는 필요 없으므로 requirements.txt와 분리한다
# (requirements.txt는 PyInstaller 빌드 환경에도 그대로 설치되는 목록이라,
# 테스트 도구가 실행 파일에 딸려 들어갈 이유가 없다).
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   python -m pytest

-r requirements.txt

pytest>=8.0

# scripts/export_clip_onnx_assets.py 전용 — CLIP 비전 인코더를 ONNX로
# 변환·양자화할 때만 필요(2026-09-13). 런타임(requirements.txt)은 onnxruntime만
# 있으면 되고 torch/open_clip의 CLIP 추론 경로는 더 이상 쓰지 않는다. 얼굴
# 탐지(facexlib)도 같은 날 완전히 제거되어 torch가 requirements.txt에서
# 빠졌으므로, 여기서 CPU 휠로 따로 명시한다 — 변환 스크립트에서만 쓰는
# 무거운 의존성을 실행 파일에 안 실을 수 있는 이유이기도 하다.
torch>=2.0
torchvision>=0.15
open_clip_torch>=2.20
onnx>=1.22
