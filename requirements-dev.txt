# 개발/테스트 전용 의존성. 앱 실행에는 필요 없으므로 requirements.txt와 분리한다
# (requirements.txt는 PyInstaller 빌드 환경에도 그대로 설치되는 목록이라,
# 테스트 도구가 실행 파일에 딸려 들어갈 이유가 없다).
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   python -m pytest

-r requirements.txt

pytest>=8.0

# scripts/fetch_deblur_assets.py · fetch_denoise_assets.py 전용.
# 이 둘만 Google Drive에서 받아오는데, 드라이브의 "바이러스 검사 못 함" 확인
# 페이지를 gdown이 대신 처리해준다. 나머지 fetch_* 스크립트는 직접 URL이라
# 표준 라이브러리만으로 받는다.
gdown>=5.0
